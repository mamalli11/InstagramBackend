export enum GenderType {
	Male = "male",
	Other = "other",
	Female = "female",
}

export enum RequestType {
	Waiting = "waiting",
	Accepted = "accepted",
	Rejected = "rejected",
}

export enum PageType {
	Public = "public",
	Private = "private",
}
