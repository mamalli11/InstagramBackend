import { MulterFile } from "src/common/utils/multer.util";

export type PostMediasType = {
	media1: MulterFile[];
	media2: MulterFile[];
	media3: MulterFile[];
	media4: MulterFile[];
	media5: MulterFile[];
	media6: MulterFile[];
	media7: MulterFile[];
	media8: MulterFile[];
	media9: MulterFile[];
	media10: MulterFile[];
};
